---
import ReadyMark from './ReadyMark.astro';
import './ProductLockup.css';

interface Props {
  variant?: 'full' | 'compact' | 'hero' | 'card' | 'hub' | 'mark' | 'sector';
  class?: string;
  decorative?: boolean;
}
const { variant = 'full', class: className = '', decorative = false } = Astro.props;
const showWordmark = variant !== 'mark';
const label = 'Clicks deploy ready';
const lockupClass = [
  'product-lockup',
  'product-brand--ready',
  variant === 'compact' && 'product-lockup--compact',
  variant === 'card' && 'product-lockup--card',
  variant === 'hub' && 'product-lockup--hub',
  variant === 'hero' && 'product-lockup--hero',
  variant === 'sector' && 'product-lockup--sector',
  className,
]
  .filter(Boolean)
  .join(' ');
---

<div
  class={lockupClass}
  dir="ltr"
  {...decorative ? { 'aria-hidden': true } : { 'aria-label': label }}
>
  <ReadyMark class="product-lockup__mark" />
  {showWordmark && (
    <>
      <span class="product-lockup__clicks">Clicks</span>
      <span class="product-lockup__name">deploy</span>
      <span class="product-lockup__badge">ready</span>
    </>
  )}
</div>
